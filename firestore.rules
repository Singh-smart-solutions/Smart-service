rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMERGENCY BYPASS: Public access for 24 hours
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
